# September 24 review

## Findings and fixes

- Production still reports 14.36 by public HTTP checks. /outbound.html returned
  the main frontend app; /backend/outbound.js returned HTML, not JavaScript.
  Git origin/maya-v2 contains the 14.37 commits, but this is not proof of deploy.
  Local Hosting route, static module path, API route and Docker copies are wired.
  Cloud Build execution/logs were not available in this session. Inspect the build
  for de1c1fc in project pro-maya before assuming another push will fix deployment.
- Outbound discarded edited drafts when changing contact, campaign, view or
  search. Added confirmation guards and a browser unload warning. Enrichment and
  generation also guard existing edits.
- Duplicate imports skipped new bounce/suppression information. Suppression now
  applies to existing email matches across the same owner's campaigns.
- Outbound now uses the actual Maya logo, Cormorant Garamond headings and Jost
  controls, visible keyboard focus, bounded mobile dialogs, wrapping long text
  and mobile header/toolbar improvements. No browser/rendered visual QA performed
  because owner prohibits computer use; these are source-level checks.
- tests/verify-live.mjs still read root index.html/status.html after their move.
  Corrected the paths and added Outbound page and JavaScript MIME/content checks.

## Request status

| Request | Current evidence / remaining work |
| --- | --- |
| Mana left flyout, Maya right flyout, new Outbound tab | Local code wired; production deployment not landed at check time. |
| Lead Station layout, pill, actions, notes, larger icons | Implemented in prior commits; source/contract tests pass. Rendered visual review still pending. |
| Affiliates dashboard and stats | Admin preview exists. Separate affiliate accounts, permissions and assigned-lead access are NOT implemented. |
| Avatar Save-as-new, editable name, slower zoom, Playground parity | Existing 22 profile/CRM checks pass; Playground experiments preserved. |
| Outbound campaigns, contacts, Hunter, Sheets, research and drafts | Implemented locally with account isolation. Provider access and real import not verified. |
| Full Explee-like automation | No automated email delivery, sequences, reply sync, mailbox warming or calendar booking. Current email handoff is manual. |
| Owner outbound workbook | Four prospect tabs inspected; row-3 corporate header and research/history mapping tested. Original sheet unchanged. No ongoing two-way sync. |
| Low-cost models / new image model | Defaults configured in prior commit; account entitlement, output quality and real token bills not verified. Environment overrides still win. |
| SMS inbound/outbound and names, block/delete | 50 fake-provider checks pass. Actual carrier delivery remains unverified; last owner evidence was A2P rejection/30034. |
| Phone secretary, transfer, lead lookup, feedback logs | 48 phone tests plus transfer/feedback pass with fake providers. No live audio/latency or log inspection this session. |
| Direct Google/Meta/Wix replacing Windsor, market research/write access | Not completed/verified by this release. Existing adapters are not proof of full account access or feature parity. |
| Pinterest broader search | Existing saved-pin pagination/search work; broad provider search access remains unverified. |
| Automatic descriptive Git commit | Prepared locally with summary/body; owner alone pushes. |

## Validation

Passed: Outbound/model 24; profile/CRM 22; proxy policy 27; AI routing 7;
fabric sourcing 6; admin command 6; admin UI 11; MCP 11; API smoke;
phone 48; SMS 50; transfer and feedback suites. JavaScript syntax and
whitespace checks pass. No paid provider calls, messages, emails, credential
changes, browser automation or production mutations.

Next: review Cloud Build history for the already-pushed 14.37 commits, resolve
its reported failure or trigger mismatch, then verify /outbound.html serves
Outbound and its module has JavaScript content type. Run tests/verify-live.mjs.
Provider connection checks and rendered visual QA are still separate checks.
