Summary:
[Codex][v14.36] Prepare descriptive local commits before owner push

Description:
Record Fromsa's standing authorization to commit completed, verified requested
work with an accurate summary and description, leaving Push to Fromsa.
Keep AGENTS.md and CLAUDE.md in sync and use Git without computer use.

Validation: matching instruction files and git diff --check. Documentation only.
The previous feature batch is already in 36368d6; its history is unchanged.
